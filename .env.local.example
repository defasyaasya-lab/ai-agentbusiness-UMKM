# Copy these values into .env.local for local setup.
# Do not commit real bot tokens, chat IDs, or production database credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=no-verify
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
